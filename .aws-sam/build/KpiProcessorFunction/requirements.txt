# RhythmCloud – Python dependencies
# These are bundled into the Lambda layer (layer/requirements.txt)
# and used locally for development and testing.

# AWS SDK
boto3==1.34.69
botocore==1.34.69

# Structured logging (AWS Lambda Powertools)
aws-lambda-powertools==2.35.1

# Schema validation
jsonschema==4.21.1

# Date/time utilities
python-dateutil==2.9.0

# Testing
pytest==8.1.1
pytest-cov==5.0.0
moto[dynamodb,s3,cloudwatch,sqs]==5.0.3
